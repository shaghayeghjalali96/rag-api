# Week 1 notes

This project intentionally does NOT use LangChain yet.

The goal is to understand the RAG pipeline directly:

PDF -> text -> chunks -> embeddings -> pgvector -> retrieval -> prompt -> LLM

After the basic version works, we can introduce LangChain/LlamaIndex where they add value.
